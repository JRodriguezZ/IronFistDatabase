<hibernate-configuration>
    <session-factory>
        <property name="hibernate.dialect"> org.hibernate.dialect.PostgreSQL9Dialect </property>
        <property name="hibernate.connection.driver_class"> org.postgresql.Driver </property>
        <!-- Assume test is the database name -->
        <property name="hibernate.connection.url"> jdbc:postgresql://localhost:5432/ironfistbbdd </property>
        <property name="hibernate.connection.username"> usuari </property>
        <property name="hibernate.connection.password"> usuari </property>
        <!-- List of XML mapping files -->
        <mapping resource="Autor.hbm.xml"/>
    </session-factory>
</hibernate-configuration>